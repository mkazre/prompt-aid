# Backend API base URL. On a physical device, `localhost` will not reach your
# dev machine — use your machine's LAN IP instead (find it with `ipconfig`).
EXPO_PUBLIC_API_URL=http://192.168.1.20:8000/api
